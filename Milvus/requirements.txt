# Milvus 向量数据库相关依赖
pymilvus>=2.3.0

# 数据处理和数值计算
numpy>=1.21.0
pandas>=1.3.0

# 语义向量化（推荐安装）
sentence-transformers>=2.2.0

# HTML 解析和网络请求
beautifulsoup4>=4.11.0
requests>=2.28.0
lxml>=4.9.0

# 网站爬虫高级功能
urllib3>=1.26.0

# LLaMA 模型支持（选择安装）
# Option 1: Ollama (推荐，轻量级)
# 需要单独安装 Ollama 服务

# Option 2: Transformers (需要GPU)
# transformers>=4.21.0
# torch>=1.12.0
# accelerate>=0.20.0

# Option 3: llama-cpp-python (CPU友好)
# llama-cpp-python>=0.2.0

# 可选：如果需要处理图像
# pillow>=8.3.0

# 可选：如果需要可视化
# matplotlib>=3.4.0
# seaborn>=0.11.0

# 可选：如果需要进度条
# tqdm>=4.62.0

# 可选：如果需要处理JSON和配置文件
# pyyaml>=6.0

# PDF 处理
PyMuPDF>=1.23.0
pillow>=8.3.0

# Web UI 依赖
flask>=2.3.0
flask-cors>=4.0.0 